rules:
  - id: insecure-sql-injection
    languages:
      - php
    severity: ERROR
    message: >
      Potential SQL Injection: avoid concatenating user input directly into SQL queries.
      Use prepared statements or parameterized queries instead.
    pattern: $SQL = $X + $Y + $Z;
